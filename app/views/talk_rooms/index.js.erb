$('#talks').html("<%= j(render partial: 'users/user-talk-index', locals: { user: @user, room: @room, talks: @talks, room_name: @room_name}) %>");

// 逆無限スクロール
$(document).ready(function(){
  let $talk = $('.user-talk-index__talks')

  // トーク内容の表示を一番下からに
  $talk.scrollTop(10000);

});
